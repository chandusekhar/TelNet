@model IEnumerable<telNet.Models.ponudaProizvoda>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ponuda.ponudaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proizvod.opisProizvoda)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.opis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ponuda.ponudaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proizvod.opisProizvoda)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.opis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ponudaProizvodaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ponudaProizvodaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ponudaProizvodaID })
        </td>
    </tr>
}

</table>
